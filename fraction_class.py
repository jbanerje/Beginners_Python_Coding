#Learning class with fraction example

class fraction:
    
     def __init__(self,num,deno):
         self.num = num
         self.deno = deno
         print(self.num,"/", self.deno) 

fra1 = fraction(1,4)
fra2 = fraction(1,2)
f1.__add__(f2)

